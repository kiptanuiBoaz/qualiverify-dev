import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#20447A',
        },
        secondary: {
            main: '#5ecaea',
        },
        text: {
            primary: '#0F172A',
            secondary: '#52525B',
        },
        background: {
            default: '#ffffff',
            paper: '#E9EFFE',

        },
    },
});

export default theme;
